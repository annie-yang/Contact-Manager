import keyMirror from 'keymirror';

// object that has keys and values that matches the keys
export default keyMirror({
  RECEIVE_CONTACTS: null, // create value that matches key
  RECEIVE_CONTACTS_ERROR: null
});
